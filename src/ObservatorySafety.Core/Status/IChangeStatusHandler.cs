
namespace ObservatorySafety.Core.Status
{

  // Capability-based split (optional but nice)
  public interface IChangeStatusHandler : IStatusHandler
  {
    // Marker interface – no extra members
  }
}
